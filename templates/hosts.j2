# {{ ansible_managed }}

127.0.0.1   localhost
::1         localhost ip6-localhost ip6-loopback

# Hosts managed by playbook

{% for item in play_hosts %}
{% set short_name = item.split('.') %}
{{ hostvars[item]['ansible_host'] }}  {{ item }}.{{ FQDN }} {{item }}
{% endfor %}
